import { StyleSheet, Text, View } from "react-native";

export default function AnimationListScreen() {
  return (
    <View style={styles.container}>
      <Text>AnimationList</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
