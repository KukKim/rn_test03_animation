import { StyleSheet, Text, View } from "react-native";

export default function AnimationTestListScreen() {
  return (
    <View style={styles.container}>
      <Text>animationTest</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
